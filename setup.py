from setuptools import setup, find_packages

setup(
    name="ahns",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="Aesthetic Harmony and Novelty Score (AHNS) evaluation framework.",
) 